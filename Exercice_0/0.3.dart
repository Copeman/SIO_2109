main() {
 var a = 3,
     b = 5,
     c = 7;
 var result = a-b/c;
 print(result);
}
// Le résultat peut-être ou non mathématiquement correct, tout dépendant du résultat désiré. Si le résultat désiré est de divisé le résultat de a soustrait de b par c, il faudra faire l’ajout de parenthèse pour indiquer les priorités, soit « var result = (a-b)/c; »
